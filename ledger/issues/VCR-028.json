{
  "id": "VCR-028",
  "title": "A dumped miss can be an empty file, and the summary's `rejected` line is named nowhere in the manual",
  "type": "defect",
  "severity": "low",
  "status": "open",
  "foundBy": "session agent replaying a multi-member campaign through cs-vcr, several replays at a time on one shared host",
  "opened": "2026-09-20",
  "resolved": null,
  "stint": null,
  "evidence": {
    "commits": [],
    "integrated": [],
    "verified": null
  },
  "resolution": null,
  "details": "**The empty dump.** With `--dump-misses`, the proxy logged `missed request written` with a file name for each miss that VCR-027 describes. Each of those files was 0 bytes long. The request was a GET with no body, so there was no body to write, but the log line says that a request was written. `cs-vcr calibrate` has nothing to read in such a file. A dump that held the method, the target and the headers would have named the cause at once. As it was, three separate readers each had to find it in the proxy log.\n\n**The `rejected` line.** The summary that `cs-vcr` prints when it stops has a `rejected` count. It moved with `misses` in every run seen. `MANUAL.md` does not hold the word. A first-time reader could not tell whether `rejected 3` was a second fault or the same one.\n\n**What is asked for.** Write the request line and the headers into every dumped miss, and keep the body as it is now. Say in the manual what `rejected` counts and how it differs from `misses`.\n\n**How to reproduce.** Run the steps in VCR-027 with `--dump-misses ./misses`, then run `ls -l ./misses` and search the output of `cs-vcr manual` for `rejected`.",
  "notes": [],
  "links": [
    "VCR-027"
  ]
}
