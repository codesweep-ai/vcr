{
  "id": "VCR-018",
  "title": "A pinned model defeats the auxiliary-turn relaxation",
  "type": "defect",
  "severity": "med",
  "status": "closed",
  "foundBy": "session agent, replaying the cs-sandbox agent matrix",
  "opened": "2026-08-28",
  "resolved": "2026-08-28",
  "stint": "2026-08-28 cs-sandbox agent replay tiers",
  "evidence": {
    "commits": [
      "dd707d5"
    ],
    "integrated": [],
    "verified": "go test ./internal/cassette/ -run 'Auxiliary|Shaped' -count=1: PASS, 5 tests, including TestAnAuxiliaryTurnIsRecognizedOnAPinnedModel and TestASessionOfOnlyShapedTurnsAbsorbsNothing. make test-smoke: 8 of 8 committed fixtures still replay."
  },
  "resolution": "A recorded step counts as bookkeeping when the cassette holds work to tell it apart from, and either signal will do: a step on another model, or a step that is not shaped like bookkeeping. The guard the model check provided is unchanged, because a cassette whose every entry is a single tool-free message still has nothing to distinguish. SPEC R8a states it.",
  "details": "`auxiliaryEntry` recognizes a recorded step as bookkeeping only when it is on a model the cassette does not otherwise use. Claude Code's title generation usually lands on haiku, so that holds for a session which lets the client choose.\n\nIt stops holding the moment a harness pins the model, and a harness that wants a replayable cassette pins the model. `claude -p --model X` puts the title call on X too, the cassette has one model throughout, and no step is recognized as auxiliary. The relaxation is then off in the one case it was written for.\n\nMeasured across the cs-sandbox agent matrix. The shared Claude cell, whose title call landed on haiku, replayed 2 of 2 requests clean. The lent cell, identical but for the credential, put the title call on the pinned model and missed one request of three, stably, across runs.\n\nThe model is the wrong axis. What separates bookkeeping from work in that cassette is the tool list: the main turn carries 21 tools and the title call carries none. `shaped` already reads the tool list, and the guard the model check provides can be had from it.",
  "notes": [],
  "links": []
}
